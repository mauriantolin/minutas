<Window x:Class="Minutas.Desktop.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Minutix Desktop"
        Icon="/Assets/minutix-mark.png"
        Width="398"
        Height="680"
        MinWidth="378"
        MaxWidth="460"
        MinHeight="560"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="TextOptions.TextFormattingMode" Value="Display" />
        </Style>

        <Style x:Key="TitleBarButton" TargetType="Button">
            <Setter Property="Width" Value="32" />
            <Setter Property="Height" Value="32" />
            <Setter Property="Foreground" Value="{DynamicResource MutedBrush}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource HoverBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CloseBarButton" TargetType="Button" BasedOn="{StaticResource TitleBarButton}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource DangerBrush}" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BaseButton" TargetType="Button">
            <Setter Property="Height" Value="42" />
            <Setter Property="Padding" Value="14,0" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="Background" Value="{DynamicResource PanelBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrushSoft}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource HoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource BaseButton}">
            <Setter Property="Background" Value="{DynamicResource PrimaryBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBrush}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource PrimaryHoverBrush}" />
                                <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource PrimaryHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="QuietButton" TargetType="Button" BasedOn="{StaticResource BaseButton}">
            <Setter Property="Height" Value="34" />
            <Setter Property="Padding" Value="12,0" />
            <Setter Property="Background" Value="{DynamicResource PanelBrush}" />
            <Setter Property="FontSize" Value="12" />
        </Style>

        <Style x:Key="LinkButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Foreground" Value="{DynamicResource MutedBrush}" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="HorizontalContentAlignment" Value="Right" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <TextBlock x:Name="Txt"
                                   Text="{TemplateBinding Content}"
                                   Foreground="{TemplateBinding Foreground}"
                                   HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" />
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Txt" Property="Foreground" Value="{DynamicResource PrimaryBrush}" />
                                <Setter TargetName="Txt" Property="TextDecorations" Value="Underline" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="InputBox" TargetType="TextBox">
            <Setter Property="Height" Value="40" />
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="Background" Value="{DynamicResource InputBgBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrushSoft}" />
            <Setter Property="CaretBrush" Value="{DynamicResource TextBrush}" />
        </Style>

        <Style x:Key="PasswordInputBox" TargetType="PasswordBox">
            <Setter Property="Height" Value="40" />
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="Background" Value="{DynamicResource InputBgBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrushSoft}" />
            <Setter Property="CaretBrush" Value="{DynamicResource TextBrush}" />
        </Style>

        <Style TargetType="ToolTip">
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrushSoft}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="MaxWidth" Value="300" />
        </Style>

        <Style x:Key="SwitchCheckBox" TargetType="CheckBox">
            <Setter Property="Width" Value="48" />
            <Setter Property="Height" Value="28" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Border x:Name="Track"
                                Width="48"
                                Height="28"
                                CornerRadius="14"
                                Background="{DynamicResource SubtleBrush}"
                                BorderThickness="0">
                            <Ellipse x:Name="Thumb"
                                     Width="20"
                                     Height="20"
                                     Margin="4"
                                     HorizontalAlignment="Left"
                                     Fill="White" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Track" Property="Background" Value="{DynamicResource PrimaryBrush}" />
                                <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ListBox">
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrushSoft}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
        </Style>

        <Style TargetType="ListBoxItem">
            <Setter Property="MinHeight" Value="36" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Row"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{DynamicResource BorderBrushSoft}"
                                BorderThickness="0,0,0,1">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Row" Property="Background" Value="{DynamicResource HoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Row" Property="Background" Value="{DynamicResource AccentBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- Thin, overlay-style scrollbar (VS Code/Orca feel), not the native chunky one. -->
        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Width" Value="8" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid Background="Transparent">
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageUpCommand" Focusable="False">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Background="Transparent" />
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Border CornerRadius="4"
                                                        Background="{DynamicResource SubtleBrush}"
                                                        Opacity="0.55"
                                                        Margin="2,0" />
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageDownCommand" Focusable="False">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Background="Transparent" />
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Margin="12"
            CornerRadius="14"
            Background="{DynamicResource BgBrush}"
            BorderBrush="{DynamicResource BorderBrushSoft}"
            BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="24" ShadowDepth="0" Opacity="0.28" Color="Black" />
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Custom title bar: drag anywhere, logo left, window buttons right. -->
            <Grid x:Name="TitleBar"
                  Grid.Row="0"
                  Background="Transparent"
                  Margin="16,10,8,2"
                  MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Image Grid.Column="0"
                       Source="/Assets/minutix-logo-cropped.png"
                       Height="26"
                       Stretch="Uniform"
                       HorizontalAlignment="Left"
                       VerticalAlignment="Center" />

                <Ellipse x:Name="HeaderStatusDot"
                         Grid.Column="2"
                         Width="8"
                         Height="8"
                         Fill="{DynamicResource SubtleBrush}"
                         VerticalAlignment="Center"
                         Margin="0,0,6,0" />

                <Button x:Name="MinimizeButton"
                        Grid.Column="3"
                        Style="{StaticResource TitleBarButton}"
                        Content="&#x2015;"
                        ToolTip="Minimizar"
                        Click="MinimizeButton_Click" />

                <Button x:Name="CloseButton"
                        Grid.Column="4"
                        Style="{StaticResource CloseBarButton}"
                        Content="&#x2715;"
                        ToolTip="Cerrar a la bandeja"
                        Click="CloseButton_Click" />
            </Grid>

            <ScrollViewer Grid.Row="1"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled">
            <Grid Margin="16,6,16,16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid x:Name="LoginPanel" Grid.Row="0" Margin="0,0,0,14">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0" Margin="0,0,0,8">
                        <TextBox x:Name="EmailBox"
                                 Style="{StaticResource InputBox}" />
                        <TextBlock x:Name="EmailPlaceholder"
                                   Text="Email"
                                   Foreground="{DynamicResource SubtleBrush}"
                                   Margin="13,0,0,0"
                                   VerticalAlignment="Center"
                                   IsHitTestVisible="False" />
                    </Grid>
                    <Grid Grid.Row="1" Margin="0,0,0,10">
                        <PasswordBox x:Name="PasswordBox"
                                     Style="{StaticResource PasswordInputBox}" />
                        <TextBlock x:Name="PasswordPlaceholder"
                                   Text="Contraseña"
                                   Foreground="{DynamicResource SubtleBrush}"
                                   Margin="13,0,0,0"
                                   VerticalAlignment="Center"
                                   IsHitTestVisible="False" />
                    </Grid>
                    <Button x:Name="SignInButton"
                            Grid.Row="2"
                            Style="{StaticResource PrimaryButton}"
                            Content="Entrar" />
                </Grid>

                <Grid x:Name="AccountPanel" Grid.Row="0" Margin="0,0,0,14" Visibility="Collapsed">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="38" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Border Width="30"
                            Height="30"
                            CornerRadius="8"
                            Background="{DynamicResource AccentBrush}"
                            VerticalAlignment="Center">
                        <TextBlock x:Name="AvatarText"
                                   Text="M"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   Foreground="{DynamicResource PrimaryBrush}"
                                   FontWeight="Bold" />
                    </Border>
                    <TextBlock x:Name="AccountEmailText"
                               Grid.Column="1"
                               VerticalAlignment="Center"
                               TextTrimming="CharacterEllipsis"
                               FontSize="14" />
                    <Button x:Name="SignOutButton"
                            Grid.Column="2"
                            Style="{StaticResource LinkButton}"
                            Content="Salir" />
                </Grid>

                <Border Grid.Row="1"
                        Background="{DynamicResource SurfaceBrush}"
                        BorderBrush="{DynamicResource BorderBrushSoft}"
                        BorderThickness="1"
                        CornerRadius="12"
                        Padding="14"
                        Margin="0,0,0,14">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Orientation="Horizontal">
                            <TextBlock Text="Captura"
                                       FontWeight="Bold"
                                       FontSize="15"
                                       VerticalAlignment="Center" />
                            <TextBlock Text="&#x24D8;"
                                       FontSize="14"
                                       Foreground="{DynamicResource MutedBrush}"
                                       Margin="7,0,0,0"
                                       VerticalAlignment="Center"
                                       Cursor="Hand"
                                       ToolTip="Lee subtítulos en vivo de Teams Desktop, incluso minimizado. No graba audio." />
                        </StackPanel>

                        <Button x:Name="StartButton"
                                Grid.Row="1"
                                Margin="0,12,0,0"
                                Style="{StaticResource PrimaryButton}"
                                Content="&#x25B6; Transcribir"
                                ToolTip="Inicia la captura de subtítulos visibles en Teams Desktop." />

                        <Grid x:Name="CapturingPanel" Grid.Row="1" Margin="0,12,0,0" Visibility="Collapsed">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <Ellipse Width="8" Height="8" Fill="{DynamicResource DangerBrush}" Margin="0,0,8,0">
                                        <Ellipse.Triggers>
                                            <EventTrigger RoutedEvent="Ellipse.Loaded">
                                                <BeginStoryboard>
                                                    <Storyboard RepeatBehavior="Forever" AutoReverse="True">
                                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                         From="1" To="0.3" Duration="0:0:0.9" />
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </EventTrigger>
                                        </Ellipse.Triggers>
                                    </Ellipse>
                                    <TextBlock Text="En vivo" FontWeight="SemiBold" />
                                    <TextBlock Text=" · " Foreground="{DynamicResource MutedBrush}" />
                                    <TextBlock x:Name="ElapsedText" Text="00:00" Foreground="{DynamicResource MutedBrush}" />
                                </StackPanel>
                                <Button x:Name="OpenLiveButton"
                                        Grid.Column="1"
                                        Style="{StaticResource LinkButton}"
                                        Content="Abrir en el navegador"
                                        ToolTip="Abre el panel web de esta reunión." />
                            </Grid>

                            <Border Grid.Row="1"
                                    Margin="0,12,0,0"
                                    Background="{DynamicResource PanelBrush}"
                                    BorderBrush="{DynamicResource BorderBrushSoft}"
                                    BorderThickness="1"
                                    CornerRadius="10">
                                <!-- Altura fija: la fila padre es Auto, asi que sin una altura concreta el
                                     ScrollViewer crece con el contenido y nunca muestra scroll. -->
                                <ScrollViewer x:Name="TranscriptScroll"
                                              Height="300"
                                              VerticalScrollBarVisibility="Auto"
                                              CanContentScroll="False"
                                              Padding="12,10">
                                    <ItemsControl x:Name="TranscriptList">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <StackPanel Margin="0,0,0,11">
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="{Binding Speaker}"
                                                                   FontWeight="SemiBold"
                                                                   FontSize="12.5"
                                                                   Foreground="{Binding Color}"
                                                                   TextTrimming="CharacterEllipsis" />
                                                        <TextBlock Text="{Binding Meta}"
                                                                   FontSize="11"
                                                                   Foreground="{DynamicResource SubtleBrush}"
                                                                   Margin="8,0,0,0"
                                                                   VerticalAlignment="Center" />
                                                    </StackPanel>
                                                    <TextBlock Text="{Binding Text}"
                                                               FontSize="13"
                                                               LineHeight="19"
                                                               Margin="0,1,0,0"
                                                               TextWrapping="Wrap"
                                                               Foreground="{DynamicResource TextBrush}" />
                                                </StackPanel>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>
                            </Border>

                            <Grid Grid.Row="2" Margin="0,12,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="10" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Button x:Name="StopButton"
                                        Style="{StaticResource PrimaryButton}"
                                        Height="38"
                                        Content="Detener"
                                        ToolTip="Finaliza la captura y pide el resumen al backend." />
                                <Button x:Name="CancelButton"
                                        Grid.Column="2"
                                        Style="{StaticResource QuietButton}"
                                        Content="Descartar"
                                        ToolTip="Detiene esta captura sin finalizar la reunión." />
                            </Grid>
                        </Grid>

                        <TextBlock x:Name="ModeText"
                                   Grid.Row="2"
                                   Text="Subtítulos de Teams Desktop"
                                   Foreground="{DynamicResource SubtleBrush}"
                                   FontSize="11"
                                   HorizontalAlignment="Center"
                                   Margin="0,10,0,0" />
                    </Grid>
                </Border>

                <TextBlock x:Name="ActivityTitle"
                           Grid.Row="2"
                           Text="Últimas reuniones"
                           FontWeight="Bold"
                           Foreground="{DynamicResource TextBrush}"
                           Margin="0,0,0,8" />

                <ListBox x:Name="ActivityList"
                         Grid.Row="3"
                         MaxHeight="200"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         MouseDoubleClick="ActivityList_MouseDoubleClick">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Height="36" Margin="8,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Title}"
                                           VerticalAlignment="Center"
                                           TextTrimming="CharacterEllipsis"
                                           FontWeight="SemiBold"
                                           Foreground="{DynamicResource TextBrush}"
                                           Margin="0,0,10,0" />
                                <TextBlock Grid.Column="1"
                                           Text="{Binding Meta}"
                                           VerticalAlignment="Center"
                                           Foreground="{DynamicResource MutedBrush}"
                                           FontSize="12" />
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <Button x:Name="OpenPanelButton"
                        Grid.Row="4"
                        Content="Abrir panel →"
                        Style="{StaticResource LinkButton}"
                        HorizontalAlignment="Right"
                        Margin="0,10,4,2" />

                <!-- Ajustes: cada detalle vive en el ⓘ (hover), no en pantalla. -->
                <Grid x:Name="AutoCaptureRow" Grid.Row="5" Margin="0,14,0,4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Automática" FontWeight="SemiBold" FontSize="13" VerticalAlignment="Center" />
                    <TextBlock Grid.Column="1"
                               Text="&#x24D8;"
                               FontSize="13"
                               Foreground="{DynamicResource MutedBrush}"
                               Margin="6,0,0,0"
                               VerticalAlignment="Center"
                               Cursor="Hand"
                               ToolTip="{Binding Text, ElementName=AutoCaptureHintText}" />
                    <TextBlock x:Name="AutoCaptureHintText"
                               Visibility="Collapsed"
                               Text="Detecta una llamada de Teams Desktop y empieza a transcribir." />
                    <CheckBox x:Name="AutoCaptureMeetingsCheckBox"
                              Grid.Column="3"
                              Style="{StaticResource SwitchCheckBox}"
                              VerticalAlignment="Center"
                              ToolTip="Cuando está activo, Minutix arranca la captura al detectar una reunión de Teams Desktop." />
                </Grid>

                <Grid x:Name="HighFidelityRow" Grid.Row="6" Margin="0,4,0,4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Alta fidelidad" FontWeight="SemiBold" FontSize="13" VerticalAlignment="Center" />
                    <TextBlock Grid.Column="1"
                               Text="&#x24D8;"
                               FontSize="13"
                               Foreground="{DynamicResource MutedBrush}"
                               Margin="6,0,0,0"
                               VerticalAlignment="Center"
                               Cursor="Hand"
                               ToolTip="{Binding Text, ElementName=HighFidelityHintText}" />
                    <TextBlock x:Name="HighFidelityHintText"
                               Visibility="Collapsed"
                               Text="Teams expone cada subtítulo con su autor exacto, desde el inicio de Windows. Reversible. También afecta a otras apps WebView2 (Outlook nuevo, Widgets)." />
                    <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button x:Name="HighFidelityButton"
                                Style="{StaticResource LinkButton}"
                                Content="Reiniciar"
                                Margin="0,0,12,0"
                                VerticalAlignment="Center"
                                ToolTip="Cierra Teams y lo vuelve a abrir para que tome el modo alta fidelidad de inmediato. No se puede hacer durante una reunión." />
                        <CheckBox x:Name="HighFidelityAutoCheckBox"
                                  Style="{StaticResource SwitchCheckBox}"
                                  VerticalAlignment="Center"
                                  ToolTip="Persiste el modo alta fidelidad (variable de entorno de usuario) para que Teams siempre exponga los subtítulos con su autor exacto. Reversible." />
                    </StackPanel>
                </Grid>

                <Grid x:Name="StartupCard" Grid.Row="7" Margin="0,4,0,4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Iniciar con Windows" FontWeight="SemiBold" FontSize="13" VerticalAlignment="Center" />
                    <TextBlock Grid.Column="1"
                               Text="&#x24D8;"
                               FontSize="13"
                               Foreground="{DynamicResource MutedBrush}"
                               Margin="6,0,0,0"
                               VerticalAlignment="Center"
                               Cursor="Hand"
                               ToolTip="{Binding Text, ElementName=StartupHintText}" />
                    <TextBlock x:Name="StartupHintText"
                               Visibility="Collapsed"
                               Text="Se abre minimizado en la bandeja." />
                    <CheckBox x:Name="StartupWithWindowsCheckBox"
                              Grid.Column="3"
                              Style="{StaticResource SwitchCheckBox}"
                              VerticalAlignment="Center"
                              ToolTip="Activa o desactiva el inicio automático de Minutix para tu usuario de Windows." />
                </Grid>

                <TextBlock x:Name="StatusText"
                           Grid.Row="8"
                           Text="Listo."
                           Foreground="{DynamicResource SubtleBrush}"
                           FontSize="11"
                           Margin="0,12,0,0" />

                <Border x:Name="UpdateCard"
                        Grid.Row="9"
                        Background="{DynamicResource SurfaceBrush}"
                        BorderBrush="{DynamicResource BorderBrushSoft}"
                        BorderThickness="1"
                        CornerRadius="12"
                        Padding="12"
                        Margin="0,12,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0"
                                   Text="Actualizaciones"
                                   FontWeight="SemiBold"
                                   FontSize="13"
                                   VerticalAlignment="Center" />
                        <TextBlock x:Name="VersionText"
                                   Grid.Column="1"
                                   Text="Versión —"
                                   Foreground="{DynamicResource MutedBrush}"
                                   FontSize="12"
                                   Margin="10,0,10,0"
                                   VerticalAlignment="Center"
                                   TextTrimming="CharacterEllipsis" />
                        <Button x:Name="UpdateButton"
                                Grid.Column="2"
                                Style="{StaticResource QuietButton}"
                                VerticalAlignment="Center"
                                Content="Buscar"
                                ToolTip="Comprueba si hay una versión nueva y la descarga. Te avisa antes de reiniciar." />
                    </Grid>
                </Border>
            </Grid>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>
